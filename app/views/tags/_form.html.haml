= form_for @tag do |f|
  - if @tag.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tag.errors.count, "error")} prohibited this tag from being saved:"
      %ul
        - @tag.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :abstract_id
    = f.text_field :abstract_id
  .field
    = f.label :value
    = f.text_field :value
  .actions
    = f.submit 'Save'
