%br
%img(src= "#{current_user.image}")
%ul
  %li= current_user.location
  %li= current_user.occupation
  
= image_tag(current_user.image) if current_user.image

= link_to 'Edit', edit_user_path(current_user)
