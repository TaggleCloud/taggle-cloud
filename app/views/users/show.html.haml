.page-header
  %h1 Welcome, #{current_user.name}
  %p
    In order to ensure that we create the best possible matches for you, be sure to
    = link_to "keep your profile updated.", edit_user_path(current_user)

- unless @conferences.empty?
  %h3 Conferences you are attending
  %ul.thumbnails
    - @conferences.each do |conference|
      %li.span4
        = link_to conference_path(conference), :class=> "thumbnail card cbutton" do
          %h4= conference.name
          %h5= conference.location
- else
  %h3.centered Looks like you aren't attending any conferences &#9785;